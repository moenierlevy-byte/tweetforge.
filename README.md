# TweetForge

Deploy:
1. Create GitHub repo, add files.
2. Import repo to Vercel.
3. Add environment variables in Vercel (OPENAI_API_KEY, STRIPE keys, SUPABASE keys).
4. Deploy and test.

If build fails, open Vercel Deployments → Logs and paste the first 12 lines here for fast help.
